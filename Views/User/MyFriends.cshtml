@model IEnumerable<CliniqonProject.Models.userDetails>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "My Friends";
}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

<div class="container mt-5">
    <h4>My Friends </h4>
    <div class="row">
        @foreach (var user in Model)
        {
            <div class="col-md-3">
                <div class="card mb-3">
                    <img src="~/PkgImage/@(user.ProfilePic)" class="card-img-top" alt="@user.Name's Profile Picture" style="height: 200px; object-fit: cover;">
                    <div class="card-body">
                        <h5 class="card-title">@user.Name</h5>
                        <p class="card-text">
                            Email: @user.Email <br />
                            Designation: @user.Designation
                        </p>
                        <a href="@Url.Action("UserDetails", "User", new { id = user.UserId })" class="btn btn-primary">View Details</a>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
